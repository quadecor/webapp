# React Frontend

This folder will contain the React frontend. You can initialize it with Create React App or Vite:

```
npx create-react-app .
# or
npm create vite@latest .
```

After setup, run:
```
npm start
```

The frontend will connect to the backend API at `/api/example`.